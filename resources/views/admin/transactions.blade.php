<x-admin-layout title="Transactions"><section class="glass-panel overflow-hidden rounded-[26px]"><div class="flex flex-wrap items-end justify-between gap-4 p-6"><div><p class="text-[11px] font-semibold uppercase tracking-[0.32em] text-sb-accent">Billing</p><h2 class="mt-2 text-2xl font-semibold text-stone-900 dark:text-stone-100">Transactions</h2></div><form method="GET"><select name="status" onchange="this.form.submit()" class="rounded-xl border border-white/10 bg-black/5 px-3 py-2 text-sm text-stone-700 dark:text-stone-200"><option value="">All statuses</option>@foreach(['pending','completed','failed','expired'] as $option)<option value="{{ $option }}" @selected($status === $option)>{{ ucfirst($option) }}</option>@endforeach</select></form></div><div class="overflow-x-auto"><table class="w-full text-left text-sm"><thead class="border-y border-white/10 text-xs uppercase text-stone-500"><tr><th class="px-6 py-3">User</th><th class="px-6 py-3">Plan</th><th class="px-6 py-3">Amount</th><th class="px-6 py-3">Status</th><th class="px-6 py-3">Created</th><th class="px-6 py-3">Expires / completed</th></tr></thead><tbody class="divide-y divide-white/10">@forelse($transactions as $transaction)<tr><td class="px-6 py-4"><p class="font-medium text-stone-900 dark:text-stone-100">{{ $transaction->user->name }}</p><p class="text-xs text-stone-500">{{ $transaction->user->email }}</p></td><td class="px-6 py-4">{{ $transaction->plan->name }}</td><td class="px-6 py-4 font-mono">₹{{ number_format($transaction->amount / 100, 2) }}</td><td class="px-6 py-4"><span class="rounded-full bg-sb-accent/10 px-2 py-1 text-xs text-sb-accent">{{ ucfirst($transaction->status) }}</span></td><td class="px-6 py-4 text-stone-500">{{ $transaction->created_at->format('M j, Y H:i') }}</td><td class="px-6 py-4 text-stone-500">{{ ($transaction->completed_at ?? $transaction->expires_at)?->format('M j, Y H:i') ?? '—' }}</td></tr>@empty<tr><td colspan="6" class="px-6 py-10 text-center text-stone-500">No transactions found.</td></tr>@endforelse</tbody></table></div><div class="border-t border-white/10 px-6 py-4">{{ $transactions->links() }}</div></section></x-admin-layout>
