<x-admin-layout title="Error logs">
    <section class="glass-panel rounded-[26px] p-6"><p class="text-[11px] font-semibold uppercase tracking-[0.32em] text-sb-accent">Diagnostics</p><h2 class="mt-2 text-2xl font-semibold text-stone-900 dark:text-stone-100">Error logs</h2>
        <div class="mt-6 space-y-3">@forelse($errors as $error)<article x-data="{ open: false }" class="rounded-2xl border border-white/10 bg-black/5"><button @click="open = !open" class="flex w-full items-center justify-between gap-4 px-4 py-4 text-left"><div><p class="font-medium text-stone-900 dark:text-stone-100">{{ $error->message }}</p><p class="mt-1 text-xs text-stone-500">{{ $error->user?->email ?? 'System' }} · {{ $error->created_at?->format('M j, Y H:i:s') }}</p></div><span @class(['rounded-full px-2.5 py-1 font-mono text-xs', 'bg-red-500/10 text-red-400' => in_array($error->level, ['error', 'critical', 'alert', 'emergency']), 'bg-amber-500/10 text-amber-400' => ! in_array($error->level, ['error', 'critical', 'alert', 'emergency'])])>{{ strtoupper($error->level) }}</span></button><div x-show="open" x-cloak class="border-t border-white/10 px-4 py-4"><p class="text-xs font-semibold uppercase tracking-wider text-stone-500">{{ $error->exception_class ?? 'Context' }}</p><pre class="mt-2 max-h-96 overflow-auto rounded-xl border border-white/10 bg-black/30 p-3 font-mono text-xs text-stone-300">{{ json_encode($error->context, JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) }}</pre></div></article>@empty<p class="rounded-2xl border border-dashed border-white/10 p-8 text-center text-sm text-stone-500">No application errors have been recorded.</p>@endforelse</div>
        <div class="mt-6">{{ $errors->links() }}</div>
    </section>
</x-admin-layout>
