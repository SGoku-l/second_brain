<x-admin-layout :title="$user->name">
    <section class="glass-panel rounded-[26px] p-6"><p class="text-[11px] font-semibold uppercase tracking-[0.32em] text-sb-accent">User detail</p><div class="mt-2 flex flex-wrap items-end justify-between gap-3"><div><h2 class="text-2xl font-semibold text-stone-900 dark:text-stone-100">{{ $user->name }}</h2><p class="text-sm text-stone-500">{{ $user->email }}</p></div><p class="font-mono text-sm text-sb-accent">{{ number_format($chunkCount) }} total chunks</p></div>
        <div class="mt-6 space-y-3">@forelse($sources as $source) @php($meta = $source->meta ?? []) <div class="rounded-2xl border border-white/10 bg-black/5 px-4 py-3"><div class="flex flex-wrap items-center justify-between gap-3"><div><p class="font-medium text-stone-900 dark:text-stone-100">{{ $source->identifier }}</p><p class="mt-1 font-mono text-xs text-stone-500">{{ $source->chunks_count }} chunks · {{ $source->type }}</p></div><span @class(['rounded-full px-2.5 py-1 text-xs font-medium', 'bg-red-500/10 text-red-400' => ($meta['status'] ?? null) === 'error', 'bg-sb-accent/10 text-sb-accent' => ($meta['status'] ?? null) !== 'error'])>{{ ucfirst($meta['status'] ?? ($source->last_synced_at ? 'indexed' : 'indexing')) }}</span></div></div> @empty <p class="rounded-2xl border border-dashed border-white/10 p-8 text-center text-sm text-stone-500">No sources in this user’s workspaces.</p> @endforelse</div>
    </section>
</x-admin-layout>
