<x-plans-layout title="Plans"><section class="glass-panel rounded-[26px] p-6"><p class="text-[11px] font-semibold uppercase tracking-[0.32em] text-sb-accent">Choose your capacity</p><h2 class="mt-2 text-2xl font-semibold text-stone-900 dark:text-stone-100">Plans that fit your workspace</h2>@if(session('subscription_notice'))<p class="mt-4 rounded-xl bg-amber-500/10 px-3 py-2 text-sm text-amber-600 dark:text-amber-300">{{ session('subscription_notice') }}</p>@endif<div class="mt-6 grid gap-4 md:grid-cols-2 lg:grid-cols-3">@forelse($plans as $plan)<article class="rounded-[26px] border border-white/10 bg-black/5 p-5"><p class="text-lg font-semibold text-stone-900 dark:text-stone-100">{{ $plan->name }}</p><p class="mt-3 font-mono text-3xl text-sb-accent">{{ $plan->is_free ? 'Free' : '₹'.number_format((float) $plan->price, 2) }}</p><p class="mt-1 text-sm text-stone-500">{{ $plan->duration_days ? $plan->duration_days / 7 .' week(s)' : $plan->duration_months.' month(s)' }}</p><ul class="mt-5 space-y-2 text-sm text-stone-500 dark:text-stone-400"><li>{{ number_format($plan->monthly_token_limit) }} tokens / month</li><li>{{ number_format($plan->monthly_repo_limit) }} repositories / month</li><li>{{ number_format($plan->storage_limit_mb) }} MB storage</li></ul><form method="POST" action="{{ route('plans.checkout', $plan) }}" class="mt-6">@csrf<button class="w-full rounded-xl bg-sb-accent px-4 py-2 text-sm font-semibold text-sb-bg-dark hover:opacity-90">{{ $plan->is_free ? 'Choose free' : 'Continue to checkout' }}</button></form></article>@empty<p class="text-sm text-stone-500">No plans are currently available.</p>@endforelse</div></section></x-plans-layout>
